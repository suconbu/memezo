rem test.txt

count = min(10,100)
if count > 10 then
	goto L_FINISH
endif

print("start.")

for i = 1 to count
	if i < 5 then
		print(i + " < 5")
	else
		print(i + " >= 5")
	endif
next i

L_FINISH:

print("finished.")
