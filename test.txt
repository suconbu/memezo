// test.txt

count = min(10,100)//minmin
if count > 10://ifif
	goto L_FINISH
endif
// ...
print("start.")//start

for i = 1 to count:
	for j = 1 to 3:
		print(j)
	endfor
	printline()
	if i == 1:
		printline(i + " is one")
	endif
	if 2 < i && i < 5:
		printline(i + " < 5")
	else:
		if i >= 5:
			printline(i + " >= 5")
		else:
			printline("else")
		endif
	endif
endfor
printline("endfor")

L_FINISH:

print("finished.")
